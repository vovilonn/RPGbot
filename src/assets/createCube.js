const { dataBase } = require("../db/dataBase");

module.exports = {
    Cube: (name, owner, id) => {
        this.name = "";
        this.id = 0;
        this.owner = "";
    },

    setCubeName: () => {
        
    },

    getCube: () => {
        // const cube = new Cube();
        // database.users.push();
    },
};
